/**
 * Built-in project-wide MCP tool implementations.
 * All tools here are global — not tied to any single team.
 * Add new tools to this barrel as they are implemented.
 */
export { ExampleTool } from './example-tool';
