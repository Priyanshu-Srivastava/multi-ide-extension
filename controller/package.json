{
  "name": "@omni/controller",
  "version": "1.0.0",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"No tests configured for @omni/controller\"",
    "lint": "eslint . --ext .ts,.tsx",
    "spec:validate": "node ../../scripts/validate-spec.js"
  },
  "dependencies": {
    "@omni/core": "*",
    "@omni/telemetry": "*"
  }
}