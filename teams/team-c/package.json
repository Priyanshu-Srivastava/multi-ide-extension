{
  "name": "@omni/team-c",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"No tests configured for @omni/team-c\"",
    "lint": "eslint . --ext .ts",
    "spec:validate": "node ../../scripts/validate-spec.js"
  },
  "dependencies": {
    "@omni/core": "*"
  }
}